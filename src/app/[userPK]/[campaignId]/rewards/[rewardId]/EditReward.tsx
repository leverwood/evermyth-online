import { Reward } from "@/app/_data/db-object-types";

interface EditRewardProps {
  reward: Reward;
}

const EditReward = ({}: EditRewardProps) => {
  return (
    <div>
      <h1>Edit Reward</h1>
    </div>
  );
};

export default EditReward;
